import { ThemeToggle, LoginForm } from "./features";
import "@/styles/global.scss";    
import "@/theme"; 

function App() {
  return (
    <main>
      <LoginForm />
    </main>
  );
}

export default App;
